template(name="categories")
  h3 Categories
  ul
    each categories
      li 
        +category

  if creating
    form(class="form-create")
      input(name="name" type="text")
      button(type="submit") Submit
      a(class="cancel" href="#") Cancel
  else
    a(class="create" href="#") Add category